<div id="user_dashboard_heading">
<h1> <%= current_user.name %> </h1>
</div>
<%= button_to "Discover Movies", "/discover", method: :get %>
<h2>My Parties</h2>
<div id="<%= current_user.id %>_viewing_parties">
<ul>
<% current_user.parties_im_hosting.each do |party| %>
  <li> Save the date: <%= party.date.strftime("%a, %b %e  %Y")%> <!-- REFACTOR - no other details are really required here, but ... would make sense --></li>
<% end %>
</ul>
</div>
